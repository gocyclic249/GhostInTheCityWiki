# Runtime dependencies for the Ghost in the City Wiki pipeline.
# Build/upload (build.py, build_html.py, upload.py) use only stdlib.
# AO3 chapter scraper (scrape.py) uses only stdlib.

# SpaceBattles scrapers — media + sidestories
requests
beautifulsoup4
lxml

# Optional: Tavily Extract API for SB image discovery (lib/tavily_utils.py).
# Falls back to direct HTTP if not installed or TAVILY_API_KEY is unset.
# tavily-python

# Optional: Selenium-based fallback image recovery (scripts/debug/*).
# Only needed if you run the manual recovery scripts.
# selenium
